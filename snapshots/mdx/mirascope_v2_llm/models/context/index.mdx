---
# AUTO-GENERATED API DOCUMENTATION - DO NOT EDIT
title: context
description: API documentation for context
---

# context

The model context manager for the `llm` module.

## <ApiType type="Alias" path="models/context/index" symbolName="LLM" /> LLM

The base interface for LLM models.

## <ApiType type="Alias" path="models/context/index" symbolName="Client" /> Client

The base interface for LLM clients.

## <ApiType type="Alias" path="models/context/index" symbolName="Params" /> Params

The base interface for LLM parameters.

## <ApiType type="Alias" path="models/context/index" symbolName="Anthropic" /> Anthropic

The Anthropic-specific implementation of the `LLM` interface.

## <ApiType type="Alias" path="models/context/index" symbolName="AnthropicClient" /> AnthropicClient

The client for the Anthropic LLM model.

## <ApiType type="Alias" path="models/context/index" symbolName="AnthropicParams" /> AnthropicParams

The parameters for the Anthropic LLM model.

## <ApiType type="Alias" path="models/context/index" symbolName="Google" /> Google

The Google-specific implementation of the `LLM` interface.

## <ApiType type="Alias" path="models/context/index" symbolName="GoogleClient" /> GoogleClient

The client for the Google LLM model.

## <ApiType type="Alias" path="models/context/index" symbolName="GoogleParams" /> GoogleParams

The parameters for the Google LLM model.

## <ApiType type="Alias" path="models/context/index" symbolName="OpenAI" /> OpenAI

The OpenAI-specific implementation of the `LLM` interface.

## <ApiType type="Alias" path="models/context/index" symbolName="OpenAIClient" /> OpenAIClient

The client for the OpenAI LLM model.

## <ApiType type="Alias" path="models/context/index" symbolName="OpenAIParams" /> OpenAIParams

The parameters for the OpenAI LLM model.

## <ApiType type="Function" path="models/context/index" symbolName="model" /> model

Set the model context with the model of the given id.

