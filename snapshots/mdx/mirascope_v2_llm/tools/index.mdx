---
# AUTO-GENERATED API DOCUMENTATION - DO NOT EDIT
title: tools
description: API documentation for tools
---

# tools

The Tools module for LLMs.

## <ApiType type="Alias" path="tools/index" symbolName="ContextTool" /> ContextTool

Tool instance with arguments provided by an LLM.

## <ApiType type="Alias" path="tools/index" symbolName="ContextToolDef" /> ContextToolDef

Protocol defining a tool that can be used by LLMs.

## <ApiType type="Module" path="tools/index" symbolName="tool" /> tool

Tool interface for LLM interactions.

## <ApiType type="Alias" path="tools/index" symbolName="Tool" /> Tool

Tool instance with arguments provided by an LLM.

## <ApiType type="Alias" path="tools/index" symbolName="ToolDef" /> ToolDef

Protocol defining a tool that can be used by LLMs.

## <ApiType type="Module" path="tools/index" symbolName="tool_def" /> tool_def

The `ToolDef` class for defining tools that LLMs can request be called.

## <ApiType type="Module" path="tools/index" symbolName="context_tool" /> context_tool

ContextTool interface for LLM interactions with context.

## <ApiType type="Module" path="tools/index" symbolName="base_tool_def" /> base_tool_def

The `BaseToolDef` class for defining tools that LLMs can request be called.

## <ApiType type="Module" path="tools/index" symbolName="decorator" /> decorator

The `llm.tool` decorator for turning functions into tools.

## <ApiType type="Module" path="tools/index" symbolName="context_tool_def" /> context_tool_def

The `ContextToolDef` class for defining tools that LLMs can request be called.

## <ApiType type="Module" path="tools/index" symbolName="base_tool" /> base_tool

BaseTool class for LLM tools.

