---
# AUTO-GENERATED API DOCUMENTATION - DO NOT EDIT
title: calls
description: API documentation for calls
---

# calls

The `llm.calls` module.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncCall" /> AsyncCall

A class for generating responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncContextCall" /> AsyncContextCall

A class for generating responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncStructuredCall" /> AsyncStructuredCall

A class for generating structured responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncStructuredContextCall" /> AsyncStructuredContextCall

A class for generating structured responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="Call" /> Call

A class for generating responses using LLMs.

## <ApiType type="Alias" path="calls/index" symbolName="ContextCall" /> ContextCall

A class for generating responses using LLMs.

## <ApiType type="Alias" path="calls/index" symbolName="StructuredCall" /> StructuredCall

A class for generating structured responses using LLMs.

## <ApiType type="Alias" path="calls/index" symbolName="StructuredContextCall" /> StructuredContextCall

A class for generating structured responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="call" /> call

The Call module for generating responses using LLMs.

