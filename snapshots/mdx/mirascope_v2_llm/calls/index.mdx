---
# AUTO-GENERATED API DOCUMENTATION - DO NOT EDIT
title: calls
description: API documentation for calls
---

# calls

The `llm.calls` module.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncCall" /> AsyncCall

A class for generating responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncContextCall" /> AsyncContextCall

A class for generating responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncStructuredCall" /> AsyncStructuredCall

A class for generating structured responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="AsyncStructuredContextCall" /> AsyncStructuredContextCall

A class for generating structured responses using LLMs asynchronously.

## <ApiType type="Alias" path="calls/index" symbolName="Call" /> Call

A class for generating responses using LLMs.

## <ApiType type="Alias" path="calls/index" symbolName="ContextCall" /> ContextCall

A class for generating responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="call" /> call

The Call module for generating responses using LLMs.

## <ApiType type="Alias" path="calls/index" symbolName="StructuredCall" /> StructuredCall

A class for generating structured responses using LLMs.

## <ApiType type="Alias" path="calls/index" symbolName="StructuredContextCall" /> StructuredContextCall

A class for generating structured responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="context_call" /> context_call

The ContextCall module for generating responses using LLMs with context.

## <ApiType type="Module" path="calls/index" symbolName="decorator" /> decorator

The `llm.call` decorator for turning `PromptTemplate` functions into LLM calls.

## <ApiType type="Module" path="calls/index" symbolName="base_context_call" /> base_context_call

The `BaseContextCall` class for LLM calls.

## <ApiType type="Module" path="calls/index" symbolName="structured_call" /> structured_call

The StructuredCall module for generating structured responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="structured_context_call" /> structured_context_call

The StructuredContextCall module for generating structured responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="async_call" /> async_call

The AsyncCall module for generating responses asynchronously using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="base_call" /> base_call

The `BaseCall` class for LLM calls.

## <ApiType type="Module" path="calls/index" symbolName="base_structured_context_call" /> base_structured_context_call

The `BaseStructuredContextCall` class for structured context LLM calls.

## <ApiType type="Module" path="calls/index" symbolName="async_context_call" /> async_context_call

The AsyncContextCall module for generating responses asynchronously using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="async_structured_call" /> async_structured_call

The AsyncStructuredCall module for generating structured responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="async_structured_context_call" /> async_structured_context_call

The AsyncStructuredContextCall module for generating structured responses using LLMs.

## <ApiType type="Module" path="calls/index" symbolName="base_structured_call" /> base_structured_call

The `BaseStructuredCall` class for structured LLM calls.

